package CreatePattern.model.magic;

import CreatePattern.model.normal.Door;
import CreatePattern.model.normal.Room;

public class MagicDoor extends Door {
	public MagicDoor( Room pre, Room after ) {

		super( pre, after );
	}
}
